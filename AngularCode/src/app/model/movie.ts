export class Movie {

    constructor(public movieName: string, public movieCategory: string, public movieProducer: string,
        public movieDirector: string, public releaseDate: string
    ) {

    }
}