export class SignUp {
    
    constructor(public userName: string, public password: string, public confirmPassword: string,
        public emailId: string, public firstName: string, public lastName: string,
        public role: string) {

    }
}