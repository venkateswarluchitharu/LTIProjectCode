export class UserDetail {
    constructor(public id: string, public userName: string, public emailId: string,
        public firstName: string, public lastname: string, public role: string) {

    }
}